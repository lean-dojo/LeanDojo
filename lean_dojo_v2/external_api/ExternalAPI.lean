import LeanCopilot

-- example (a b c : Nat) : a + b + c = a + c + b := by
--   suggest_tactics_kimina

-- example (a b c : Nat) : a + b + c = a + c + b := by
--   suggest_tactics_deepseek

-- example (a b c : Nat) : a + b + c = a + c + b := by
  -- suggest_tactics
